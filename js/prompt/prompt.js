// prompt is a function that shows a modal window that asks for user input, an input field and the buttons OK or Cancel.
let result = +prompt("Enter a number")

// this will return the result in a modal window.
alert(result)