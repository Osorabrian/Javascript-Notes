// Confirm function shows a modal window with a text message and two button OK and cancel.
// If the user presses OK the result is TRUE but if it is Cnacel the result is FALSE.

let result = confirm("Are you Brian?")

alert(result)